#List all houses that have a guestroom and are located on the main road.

SELECT price, area, bedrooms, bathrooms, stories
FROM `windy-cedar-451319-q6.Housing_data.Housing`
WHERE guestroom = true AND mainroad = true;
